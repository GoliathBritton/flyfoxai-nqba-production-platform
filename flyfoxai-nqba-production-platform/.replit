run = """
pip install -r backend/requirements.txt
cd backend && uvicorn main:app --host 0.0.0.0 --port 8000 &
cd frontend && npm install && npm run dev
"""
hidden = [".git", "node_modules", "__pycache__"]
